# SPEC — Ω Autonomous Leadership & Project Assurance System v3.0

## الهدف
إنشاء نظام قيادة ذاتية عام يجعل أي مشروع أو نية قابلة للفهم، التخطيط، التنفيذ، التحقق، التعافي، الاستئناف، والتسليم بأقل تدخل بشري ممكن، دون الاعتماد على ذاكرة محادثة واحدة أو أداة واحدة.

## النطاق
يشمل: Intent Intake، مصدر حقيقة خارجي، Leadership Gate، State، قرارات، قدرات، Evidence، توليد مهام، NBAG، تنفيذ فعلي، اختبارات عادية وعدائية وRegression، Recovery/Retry/Failover، Health Check، Checkpoints، CI، Self-Chain، Watchdog، وRelease/Delivery Gate.

## غير النطاق
لا يفترض النظام استمرار ChatGPT في الخلفية، ولا يمنح نفسه صلاحيات غير موجودة، ولا يتجاوز سياسات أو حدود خدمة، ولا ينشئ التزامات مالية أو إجراءات عالية الأثر غير القابلة للعكس بلا سلطة صريحة.

## معايير القبول
1. يمكن لوكيل جديد فهم الغاية والحالة من الملفات دون الرجوع للمحادثة.
2. توجد قيادة إلزامية قبل التنفيذ تنتج قرارًا قابلًا للتدقيق وتفحص أسئلة الغاية والنجاح والمجهولات والقيود والمخاطر والدليل والبدائل والحاجة للإنسان.
3. لا يصل NBAG أو Executor إلى التنفيذ إذا كانت قيادة المهمة SAFE_BLOCKED أو HUMAN_REQUIRED.
4. تُستخدم افتراضات آمنة وقابلة للعكس لتقليل العبء البشري، وتُسجل بدل إخفائها.
5. يوجد فحص صحة قابل للتشغيل محليًا وداخل GitHub Actions.
6. كل اعتماد لأداة له اختبار قدرة فعلي أو حالة UNVERIFIED واضحة.
7. يوجد مسار أساسي ومسار بديل موثق، وRecovery/Failover بميزانية محاولات محدودة.
8. يوجد سجل Evidence يربط الادعاءات بالـcommit والاختبار والـartifact/التشغيل عند انطباقه.
9. توجد اختبارات عادية وعدائية وRegression واختبارات للقيادة وNBAG والاستمرار.
10. لا يعلن النظام GO/COMPLETE/RELEASED عند غياب أي شرط جوهري.
11. يمكن استعادة العمل من آخر Known Good Commit ولا يضيع Intent بانقطاع الهاتف أو المحادثة.
12. لا توجد أسرار أو مفاتيح داخل المستودع ولا يستطيع الوكيل توسيع صلاحياته أو تعطيل بوابات الحوكمة.
13. بعد كل تنفيذ يعاد تقييم الغاية والحالة؛ إذا ظهر مسار أفضل لا تُستكمل الخطة القديمة لمجرد أنها بدأت.
14. عند وجود عمل قابل للتنفيذ تستمر الدورة تلقائيًا؛ التوقف المسموح فقط Terminal Candidate مثبت أو SAFE_BLOCKED موثق.
15. عند وجود منتج قابل للنشر: Build → Release Gate → Deploy → Smoke Test → Evidence → Delivery.

## سياسة القرار
FACT = دليل مباشر. INFERENCE = استنتاج معلن. ASSUMPTION = غير مثبت ويجب أن يكون آمنًا وقابلًا للعكس إذا استُخدم. لا يُعامل ASSUMPTION كـFACT.

الأولوية: تحقق النية > إزالة blocker > إصلاح الفشل > إكمال الاعتماد الحرج > التحقق > الميزات غير الجوهرية. عند التعادل اختر الأقل خطرًا والأكثر قابلية للعكس والأقل كلفة.

## بوابة الإصدار
GREEN/GO فقط إذا نجح Leadership Gate وHealth Check وRegression، ووجد Evidence وArtifact عند انطباقه، وثُبت commit، ولا يوجد blocker عالي التأثير، ونجح Smoke Test عند وجود نشر.
YELLOW/CONDITIONAL GO فقط بقرار موثق يحدد المخاطر المقبولة ولا يخالف شرطًا أمنيًا أو قبولًا جوهريًا.
RED/NO-GO عند فشل شرط جوهري أو غياب الدليل.
