# SPEC — Ω Project Autonomy & Assurance System v2.0

## الهدف
إنشاء هيكل تشغيل عام يجعل أي مشروع قابلًا للاستئناف والتدقيق والاختبار دون الاعتماد على ذاكرة محادثة واحدة أو أداة واحدة.

## النطاق
يشمل: مصدر حقيقة خارجي، State، قرارات، قدرات، Evidence، اختبارات، اختبارات عدائية، Health Check، بدائل، Checkpoints، تشغيل محلي، CI، وجدول تحقق دوري، وبوابة GO/NO-GO.

## غير النطاق
لا يفترض النظام استمرار ChatGPT في الخلفية، ولا يمنح صلاحيات غير موجودة، ولا يتجاوز سياسات أو حدود خدمة.

## معايير القبول
1. يمكن لوكيل جديد فهم الحالة من الملفات دون الرجوع للمحادثة.
2. يوجد فحص صحة قابل للتشغيل محليًا وداخل GitHub Actions.
3. كل اعتماد لأداة له اختبار قدرة فعلي أو حالة UNVERIFIED واضحة.
4. يوجد مسار أساسي ومسار بديل محلي.
5. يوجد سجل Evidence يربط الادعاءات بالـcommit والاختبار.
6. توجد اختبارات عادية وعدائية وRegression.
7. يوجد تشغيل مجدول دوريًا للحفاظ على الاستمرارية.
8. لا يعلن النظام GO عند غياب أي شرط جوهري.
9. يمكن استعادة العمل من آخر Known Good Commit.
10. لا توجد أسرار أو مفاتيح داخل المستودع.

## سياسة الجودة
FACT = دليل مباشر. INFERENCE = استنتاج معلن. ASSUMPTION = غير مثبت. لا يُعامل ASSUMPTION كـFACT.

## بوابة الإصدار
GREEN/GO فقط إذا نجح Health Check وRegression، ووجد Artifact وEvidence، وثُبت commit، ولا يوجد blocker عالي التأثير.
YELLOW/CONDITIONAL GO فقط بقرار موثق يحدد المخاطر المقبولة.
RED/NO-GO عند فشل شرط جوهري أو غياب الدليل.
