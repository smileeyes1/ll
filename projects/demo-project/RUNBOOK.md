# Runbook

1. Read `STATE.md`.
2. Verify the last known-good commit and CI evidence.
3. Read `SPEC.md` and `DECISIONS.md`.
4. Run the local tests.
5. Run the release gate.
6. Record the result in `STATE.md` and `EVIDENCE.md`.

Interruption recovery never requires the previous chat.
