# {{PROJECT_NAME}}

Generated by Ω Project Factory.

## First-run
1. Read `AGENT.md`.
2. Complete `SPEC.md`.
3. Record current state in `STATE.md`.
4. Verify every required tool before adoption.
5. Run `bash scripts/health-check.sh`.
6. Run `bash tests/test-structure.sh` and `bash tests/test-adversarial.sh`.

## Continuity
This project does not depend on chat history. `SPEC.md`, `STATE.md`, `DECISIONS.md`, `CAPABILITIES.md`, `EVIDENCE.md`, tests and artifacts are authoritative.
